using UnityEngine;

public class DepthUIScript : MonoBehaviour
{
    public float depth;

    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}